/** =================================================================================================================
   * 
   * async/await est une facon géniale d'écrire les promesses.
   * 
   * ------------------------------------------------------------------------------------------------------------------
   * 
   * Un async/await peut réussir ou échouer (comme un promesse).
   * Un async/await peut utiliser une fonction qui renvoie une promesse.
   *
   * --------------------------------------------------------------------------
   * 
   * => Toutes les promesses PEUVENT et DOIVENT etre remplacées par des async/await
   * --------------------------------------------------------------------------
   * Les promesses servent très majoritairement pour appeler des fonctions asynchrones (ex. : lecture/écriture de fichiers, requêtes API, ...)
   * Ici, on ne verra que des appels synchrones
   * =============================================================================================================== */

(() => {
  // TODO
})()